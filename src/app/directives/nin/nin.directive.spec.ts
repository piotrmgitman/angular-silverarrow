import { NinDirective } from './nin.directive';

describe('NinDirective', () => {
  it('should create an instance', () => {
    const directive = new NinDirective();
    expect(directive).toBeTruthy();
  });
});
